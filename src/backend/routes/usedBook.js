// import {
//   [handlers]
// } from "../controllers/usedBook.js";
import express from "express";

// Create an express router
const router = express.Router();

// Every path we define here will get /api/usedbook prefix as defined in index.js

// POST /api/usedbook
// router.post("/", [handler]); // Here "/" translates to /api/usedbook

// export the router
export default router;
